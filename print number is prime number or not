#include <stdio.h>
#include <math.h>
int main() {
    // Write C code here
    int i,n,c=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(n%i==0)
        c=c+1;
    }
    if(c==2)
        printf("prime number");
        else
        printf("not a prime number");
    }

